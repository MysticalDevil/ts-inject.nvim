# archived generated-host static queries

This directory stores historical static `injections.scm` snapshots for hosts
that now use generated runtime queries.

These files are reference-only and are not loaded at runtime.

Current generated hosts with archived static snapshots:

- `python`
- `javascript`
- `typescript`
- `lua`
- `ruby`
